# Proofs: Task 06 — Validate and capture proof artifacts

Covers: all

## Planned evidence

- `./mvnw test` full output confirming `BUILD SUCCESS`.
- JaCoCo coverage report screenshot or excerpt showing ≥90% line coverage on
  `ResourceNotFoundException` and the three modified controllers.
- `cd e2e-tests && npm test -- --grep "Owner Management"` full output confirming
  all tests pass.
- Confirmation that `owner-not-found.png` was written to the Playwright artifacts
  directory.
- Updated coverage matrix in `07-validation-friendly-404.md` with all rows set
  to `PASS`.
- Ticked Definition of Done checklist in `07-validation-friendly-404.md`.

## Completion notes

(Filled in by `implement-sdd-spec`.)
