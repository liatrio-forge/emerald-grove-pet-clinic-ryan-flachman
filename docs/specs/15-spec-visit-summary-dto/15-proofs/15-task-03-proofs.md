# Proofs: Task 03 — Write failing tests for VisitSummary record (RED)

Covers: AC-2.b, AC-2.c, AC-3.a, AC-3.b, AC-3.c, AC-3.d, AC-3.e, AC-3.f, AC-4.a, AC-4.b

## Planned evidence

- `VisitSummaryTest.java` created at
  `src/test/java/org/springframework/samples/petclinic/owner/VisitSummaryTest.java`.
- Compile output showing test class fails to compile (or test run showing
  `ClassNotFoundException` / compilation error) because `VisitSummary` does not
  yet exist — confirming RED state.

## Completion notes

(Filled in by `implement-sdd-spec`.)
