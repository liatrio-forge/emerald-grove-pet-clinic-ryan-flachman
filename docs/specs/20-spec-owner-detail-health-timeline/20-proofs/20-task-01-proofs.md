# Proofs: Task 01 — Write failing OwnerControllerTests test

Covers: AC-3.a, AC-3.b, AC-3.c

## Planned evidence

- Diff of `OwnerControllerTests.java` showing the new `testOwnerDetailsContainsHealthTimelineToggle()` method.
- `./mvnw test -Dtest=OwnerControllerTests` output showing the new test failing (RED phase) before `ownerDetails.html` is modified.

## Completion notes

(Filled in by `implement-sdd-spec`.)
