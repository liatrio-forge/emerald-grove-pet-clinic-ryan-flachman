# Proofs: Task 02 — Update ownerDetails.html with collapse toggle and fragment insert

Covers: AC-1.a, AC-1.b, AC-2.a, AC-2.b, AC-2.c

## Planned evidence

- Diff of `ownerDetails.html` showing the Bootstrap collapse toggle element and `th:insert` addition inside the per-pet loop.
- `grep` output confirming `health-timeline :: healthTimeline`, `Health Timeline`, pet-scoped id, and `data-bs-target` are present.
- `./mvnw test -Dtest=OwnerControllerTests` output showing the previously failing test now passing (GREEN phase).

## Completion notes

(Filled in by `implement-sdd-spec`.)
