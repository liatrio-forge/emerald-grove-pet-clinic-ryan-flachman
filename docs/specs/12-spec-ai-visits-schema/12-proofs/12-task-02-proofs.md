# Proofs: Task 02 — Update H2 and HSQLDB CREATE TABLE visits blocks

Covers: AC-1.a, AC-1.b, AC-1.c, AC-2.a, AC-2.b

## Evidence

### H2 schema (`db/h2/schema.sql`)

```sql
CREATE TABLE visits (
  id          INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  pet_id      INTEGER,
  visit_date  DATE,
  description VARCHAR(2000),
  ai_status   VARCHAR(20) DEFAULT 'PENDING',
  ai_summary  VARCHAR(1000),
  ai_tags     VARCHAR(500),
  ai_urgency  VARCHAR(20),
  ai_follow_up VARCHAR(500)
);
```

### HSQLDB schema (`db/hsqldb/schema.sql`)

Same block with `INTEGER IDENTITY PRIMARY KEY` syntax — all five AI columns and `VARCHAR(2000)` description present.

### VisitsSchemaIT GREEN output

```text
Tests run: 2, Failures: 0, Errors: 0, Skipped: 0
BUILD SUCCESS
```

Also updated `db/h2/data.sql` and `db/hsqldb/data.sql` seed INSERTs to use named-column syntax to prevent positional-value mismatch after column additions.
