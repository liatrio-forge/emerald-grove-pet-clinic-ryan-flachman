# Proofs: Task 01 — Write failing schema-verification test (RED)

Covers: AC-6.a, AC-6.b

## Planned evidence

- `src/test/java/.../owner/VisitsSchemaIT.java` exists.
- `./mvnw test -Dtest=VisitsSchemaIT` output shows the test failing with an error
  indicating the AI columns or description length constraint do not yet exist in
  the schema.

## Completion notes

(Filled in by `implement-sdd-spec`.)
