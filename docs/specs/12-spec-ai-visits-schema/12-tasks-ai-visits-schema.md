# Tasks: AI Visits Schema (12)

## Parent task plan (awaiting confirmation)

1. Write failing schema-verification test `VisitsSchemaIT` (RED) — covers AC-5.a, AC-6.a, AC-6.b
2. Update H2 and HSQLDB `CREATE TABLE visits` blocks (GREEN) — covers AC-1.a, AC-1.b, AC-1.c, AC-2.a, AC-2.b
3. Append idempotent `ALTER TABLE` statements to MySQL schema — covers AC-3.a, AC-3.b
4. Append idempotent `ALTER TABLE` statements to PostgreSQL schema — covers AC-4.a, AC-4.b
5. Validate and capture proof artifacts — covers all
