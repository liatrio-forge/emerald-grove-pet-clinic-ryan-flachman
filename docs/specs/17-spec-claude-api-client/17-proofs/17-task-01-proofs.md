# Proofs: Task 01 — Write failing construction tests for all four POJOs (RED)

Covers: AC-2.d, AC-3.c, AC-4.d, AC-5.c, AC-6.a

## Planned evidence

- Maven output from running the four POJO test classes against an empty
  codebase — should fail to compile because `ClaudeRequest`, `Message`,
  `ClaudeResponse`, and `ContentBlock` do not exist yet.
- The compile error output confirms the RED phase is genuine.

## Completion notes

(Filled in by `implement-sdd-spec`.)
