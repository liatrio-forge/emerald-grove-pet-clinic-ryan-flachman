# Proofs: Task 04 — Write failing `VisitSummaryServiceTests` (RED)

Covers: AC-15.b

## Planned evidence

- Maven output from `./mvnw test -Dtest=VisitSummaryServiceTests` showing a
  compilation failure because `VisitSummaryService` does not yet exist (RED phase).

## Completion notes

(Filled in by `implement-sdd-spec`.)
