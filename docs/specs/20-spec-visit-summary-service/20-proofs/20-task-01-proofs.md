# Proofs: Task 01 — Write failing `VisitSummaryParserTests` (RED)

Covers: AC-15.a

## Planned evidence

- Maven output from `./mvnw test -Dtest=VisitSummaryParserTests` showing a
  compilation failure or test-not-found error because `VisitSummaryParser` and
  `VisitSummaryParseException` do not yet exist.

## Completion notes

(Filled in by `implement-sdd-spec`.)
