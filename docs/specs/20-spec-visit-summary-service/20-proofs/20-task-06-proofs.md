# Proofs: Task 06 — Validate and capture proof artifacts

Covers: all (AC-16.a, AC-16.b, full regression)

## Planned evidence

- Maven output from `./mvnw test` showing `BUILD SUCCESS`, 0 failures, 0 errors.
- JaCoCo coverage summary (from `./mvnw test jacoco:report`) showing ≥ 90% line
  coverage for `VisitSummaryParser` and `VisitSummaryService`.
- All structural grep outputs from `20-validation-visit-summary-service.md`.

## Completion notes

(Filled in by `implement-sdd-spec`.)
