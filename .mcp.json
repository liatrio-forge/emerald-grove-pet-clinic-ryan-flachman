{
  "mcpServers": {
    "dbhub-petclinic": {
      "type": "stdio",
      "command": "npx",
      "args": [
        "-y",
        "@bytebase/dbhub@latest",
        "--transport",
        "stdio",
        "--dsn",
        "postgres://petclinic:petclinic@localhost:5432/petclinic?sslmode=disable"
      ],
      "env": {}
    }
  }
}
